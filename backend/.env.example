# Required: Anthropic API key for Claude AI job scoring
ANTHROPIC_API_KEY=sk-ant-...

# Optional: comma-separated list of allowed CORS origins (defaults to * in dev)
# ALLOWED_ORIGINS=https://your-app.vercel.app
